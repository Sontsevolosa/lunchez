##Customize the test machine
machine:
  
  #Version of ruby to use
  ruby:
    version:
      2.3.1

## Customize dependencies
dependencies:
  pre:
    - gem install bundler --pre

  override:
    - bundle install:
        timeout: 180